# Uninstall

```bash
peektea uninstall
```

For when you've had your fill.

It shows you exactly what it's about to delete, asks for confirmation, then asks separately whether you also want `~/.peektea.toml` removed too.

No silent nuke — no tea spilled without warning.
