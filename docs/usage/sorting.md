# Sorting

![sort and preview scroll demo](../media/sort_and_preview_scroll.gif)

Press `s` to cycle the sort mode:

```
name → size (largest first) → modified (newest first) → back to name
```

The current sort mode is always shown in the hint bar, so you never lose track of how the list is ordered.
